# apps

Frontend applications (React + TypeScript).

- [`web-frontend/`](web-frontend/) — the operator single-page application. It talks only to the BFF
  gateway and is served in production as static assets by Caddy. Added in EP-1 (TASK_01E-2:
  dev-login and the appeal list/search); extended in later 01E subtasks.
