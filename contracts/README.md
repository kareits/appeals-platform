# contracts

Versioned OpenAPI and JSON Schema contracts, validated in CI. The event-envelope schema is added
in TASK_00C; per-service OpenAPI and event schemas follow as their services are implemented.
Change contracts before implementation (contract-first). Empty during TASK_00A.
