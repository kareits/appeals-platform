# infrastructure

Docker Compose and deployment assets. The local stack (PostgreSQL, RabbitMQ, Flowable, reverse
proxy) is added in TASK_00B (EP-0). Production configuration, secrets, TLS, and backup/restore
arrive in EP-7. Empty during TASK_00A.
