# Alembic configuration for the IAM service.
# The database URL is resolved in migrations/env.py from the Alembic config or application
# settings, so no static credentials are stored here.

[alembic]
script_location = migrations
prepend_sys_path = src
path_separator = os
